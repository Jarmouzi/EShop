@model EShop.ViewModel.CategoryViewModel
<form id="create-form" method="post" asp-page="Index" asp-route-id="@Model.Id" asp-page-handler="CreateOrEdit" onsubmit="return jQueryModalPost(this);" class="form-horizontal">
@*     <input type="hidden" asp-for="CreateDate" /> *@
    <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
    <div class="form-group col-md-6">
        <label class="control-label">گروه بندی سطح 1</label>
        <select asp-for="GrandParentId" asp-items="Model.PrimaryCategories" class="form-control" onchange="SetCategories(this.value);">
            <option value="">انتخاب نمایید</option>
        </select>
    </div>
    <div class="form-group col-md-6">
        <label asp-for="ParentId" class="control-label">گروه بندی سطح 2</label>
        <select asp-for="ParentId" asp-items="Model.SecondaryCategories" class="form-control">
            <option value="">انتخاب نمایید</option>
        </select>
    </div>
    <div class="form-group col-md-6">
        <label class="control-label">نام دسته بندی</label>
        <input type="text" autocomplete="off" asp-for="Title" name="Title" class="form-control">
        <span asp-validation-for="Title" class="text-danger"></span>

    </div>
    <div class="form-group col-md-12 pull-left">        
        <div class="pull-left">
            <a onclick="clearForm()" class="btn btn-default">خالی کردن فرم</a>
            <button id="saveBtn" type="submit" class="btn btn-success">ایجاد</button>
            <a onclick="Search(0);" class="btn btn-info">جستجو</a>
        </div>
    </div>

</form>
<script type="text/javascript" language=javascript>
    $.validator.unobtrusive.parse(document);

</script>