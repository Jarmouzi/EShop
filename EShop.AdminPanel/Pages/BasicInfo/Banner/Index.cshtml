@page
@model EShop.AdminPanel.Pages.BasicInfo.Banner.IndexModel
@{
    ViewData["Title"] = "Banner";
}
@await Component.InvokeAsync("Breadcrumb")
<section class="content">
    <div class="row">
        <div class="col-md-12">
            <div class="box box-info">
                <div class="box-header with-border">
                    <div id="bannerForm" class="row"></div>
                </div>
                <div class="box-body">
                    <div class="row">
                        <div id="viewAll" class="card-body table-responsive col-sm-12"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts
    {
    <script>
        $(document).ready(function () {
            $('#bannerForm').load('?handler=FormPartial');
            $('#viewAll').load('?handler=ViewAllPartial&title=&country=&take=10&skip=0');
        });

        clearForm = () => {
            $('#create-form').attr('action', '/Banner?id=&handler=CreateOrEdit');  
			$('#Confirmed).val(');  
			$('#Image).val(');  
			$('#Path).val(');  
			$('#Title).val('); 
            $('#saveBtn').html('?????');
        }

        Search = (take, skip) => {  
			var Confirmed = $("input#Confirmed").val(); 
			var Image = $("input#Image").val(); 
			var Path = $("input#Path").val(); 
			var Title = $("input#Title").val();
            $('#viewAll').load(`?handler=ViewAllPartial&&amp;Confirmed=${Confirmed}&amp;Image=${Image}&amp;Path=${Path}&amp;Title=${Title}&take=${take}&skip=${skip}`);
        }
    </script>
}
