@page
@model EShop.AdminPanel.Pages.BasicInfo.State.IndexModel
@{
    ViewData["Title"] = "State";
}
@await Component.InvokeAsync("Breadcrumb")
<section class="content">
    <div class="row">
        <div class="col-md-12">
            <div class="box box-info">
                <div class="box-header with-border">
                    <div id="stateForm" class="row"></div>
                </div>
                <div class="box-body">
                    <div class="row">
                        <div id="viewAll" class="card-body table-responsive col-sm-12"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts
    {
    <script>
        $(document).ready(function () {
            $('#stateForm').load('?handler=FormPartial');
            $('#viewAll').load('?handler=ViewAllPartial&Title=&amp;take=10&skip=0');
        });

        clearForm = () => {
            $("#create-form").attr("action", "/State?id=&handler=CreateOrEdit"); 
            $(".validation-summary-errors").val("");
            $("field-validation-error").val("");  
			$("#Title").val(""); 
            $("#saveBtn").html("?????");
        }

        Search = (take, skip) => {  
			var Title = $("input#Title").val();
            $("#viewAll").load(`?handler=ViewAllPartial&&amp;Title=${Title}&take=${take}&skip=${skip}`);
        }
    </script>
}
