@using EShop.ViewModel;
@model PaginatedViewModel<Supplier_ContractViewModel>

<table class="table table-bordered" id="supplier_contracts">
    <thead>
        <tr>
            <th>عملیات</th>
             
					<th>CategoryId</th> 
					<th>درصد سود سامانه از فروش</th> 
					<th>Confirmed</th> 
					<th>SaleTypeId</th> 
					<th>SupplierId</th> 
					<th>Title</th>  
        </tr>
    </thead>
    <tbody>
        @if (Model.Data.Any())
        {
            @foreach (var m in Model.Data)
            {
                <tr>
                    <td class="d-inline">
                        <form method="post" asp-page="Index" asp-route-id="@m.Id" asp-page-handler="Delete" onsubmit="return jQueryDelete(this)">
                            <button type="submit" data-toggle="tooltip" data-placement="top" title="???" class="d-link"> <i class="fa fa-remove"></i></button>
                        </form> |
                        <a onclick="jQueryGet('?handler=FormPartial&id=@m.Id',' + LOWER(TABLE_NAME) + N'Form', '??????')" data-toggle="tooltip" data-placement="top" title="??????"> <i class="fa fa-edit"></i> </a>

                    </td> <td>@m.SupplierId</td><td>@m.SaleTypeId</td><td>@m.CategoryId</td><td>@m.Commission</td> <td>
                        @if (m.Confirmed == true)
                        {
                            <input type="checkbox" checked="checked" disabled="disabled" />
                        }
                        else
                        {
                            <input type="checkbox" disabled="disabled" />
                        }
                    </td> <td>@m.Title</td>                 
                    
                </tr>
            }
        }
        else
        {
            <tr>
				<td colspan="7">
                ?????? ???? ???
				</td>
            </tr>

        }
    </tbody>
</table>
<partial name="_paginationpartial" model="@Model.Pagination" />
<script>
    $(document).ready(function () {
        $("#supplier_contractTable").DataTable();
    });
</script>
